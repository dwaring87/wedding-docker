# Static Access Token that is given to the Nuxt user
# on the CMS that is used by the Nuxt website API
# to access the database
#
# This token uses the API access policy, which has
# read access to most tables and has update access
# to the guest table (to update RSVP info)
NUXT_DIRECTUS_TOKEN=CHANGE_TO_RANDOM_STRING
